The current task is to resolve two Kotlin test failures: adaptive RSI calibration returns no selected threshold where the test expects a 30.0 ceiling, and Groww watchlist sync uses `holding` where the expected grouping is `groww_HIGH_QUALITY`. The repository is a Kotlin Maven multi-module project; both failures are in `core` and should be fixed with small, contract-preserving changes.

Initial evidence: `GrowwWatchlistSyncRequest` defaults to `IndexConstituentKeys.GROWW_WATCHLIST`, currently defined as `holding`, while the sync test expects `groww_HIGH_QUALITY`. The RSI calculator filters calibration events by confirmation/holding lookahead and applies an event-spacing rule after successful samples; focused test output and config defaults will determine whether the regression is in the calibration sample window or candidate selection. Validation will include focused Maven tests, relevant core tests, and a Kotlin/code review pass.
