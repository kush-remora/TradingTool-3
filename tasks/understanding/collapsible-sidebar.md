# Collapsible application sidebar

The application sidebar is useful for route switching but consumes horizontal space on dense screens such as Compact Stock Review. The shared app shell now provides one accessible header toggle: expanded mode keeps the current 260px menu, while collapsed mode leaves a 72px icon rail. The route, selected menu item, calculator, and page content are unchanged.
