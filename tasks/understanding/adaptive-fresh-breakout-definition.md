# Adaptive Fresh Breakout Definition

The goal is to detect the first daily close above a meaningful consolidation ceiling without assuming that every stock absorbs supply for 20, 30, or 40 sessions. Breakout detection should model price structure only; volume, close strength, follow-through, and fake-breakout rejection remain separate confirmation stages. Phase 1 is a latest-date watchlist scanner, not a backtest.

The proposed direction is an ATR-adaptive structural resistance model replayed oldest-to-newest. A prior swing high becomes an active ceiling only after price meaningfully retreats from it; the ceiling includes a small volatility-based allowance. A fresh breakout occurs once, when today's close crosses above the active ceiling while yesterday's close was at or below it. The base duration is then an observed output—from ceiling formation to breakout—not a configured lookback.

The scanner must return every watchlist stock, its current state, latest completed candle, active ceiling, and raw candle-by-candle decisions for manual validation. The UI must also display the latest available Kite LTP and distinguish an intraday live breakout candidate from a completed-close breakout. Close-near-high, volume versus the prior 10-session average, and distance from the 52-week high are evidence-only fields for now and must not affect the core decision.

An uninterrupted rebound such as `100 → 80 → 81 → 82 → 83 → 84 → 85` has no post-floor rejection and therefore no local ceiling. Below two ATR it is `NO_CEILING`; at two ATR or more it is `STRONG_REBOUND`. Neither state is a breakout, and the scanner must never invent a ceiling merely to produce a signal.

Implemented on 2026-08-15 as an Adaptive Breakout Scanner with a watchlist run action, latest completed-close status, Kite LTP overlay, and raw replay audit drawer. Focused engine/UI tests and backend/frontend compilation passed. The important correction from review was to demote distant pre-decline resistance to major overhead after a new local floor is established; it remains visible but cannot block formation and breakout of a nearer local ceiling.

UI follow-up: the daily watchlist picture must be understandable in one viewport without reading across many raw numeric columns. The primary hierarchy is actionable state, current price, and distance to the ceiling; confirmation evidence is compressed into visual cues; counts double as quick filters; raw replay moves to a secondary drawer so it never disrupts the overview.

Stock navigation follow-up: keep the scanner as the overview rather than duplicating the existing stock-detail experience. The stock symbol should open its Kite chart in a new tab, while a small secondary `Stock detail` link should open the existing Compact Stock Review for that row's symbol in a new tab.

Confirmation-source correction: a live candidate uses the current Kite candle and is labelled unconfirmed. Once NSE market hours have ended, a current-dated Kite snapshot may act as the completed close, including LTP, day high/low, and volume. Fresh and already-broken rows show evidence from the actual breakout candle; other states show the latest completed close. The UI must label this source explicitly so evidence from different dates cannot be mistaken for today's candle.

Breakout-evidence persistence correction: confirmation data for a completed breakout must be carried explicitly in the scan response. It cannot depend on finding the breakout inside the truncated raw replay window. If evidence is unexpectedly absent, show the breakout date with unavailable values; never silently substitute the latest candle.

Detail-popup follow-up: each scanner row should offer the same recent 20-session OHLC, volume/10D, delivery, change, close-position, and daily-range table used by the 5-Day Stock Selector. This should be one shared UI component rather than two independently evolving copies. Both detail popup and raw decision replay must be non-modal; raw replay must start below the global header so the calculator remains visible and usable.

Local-structure correction: a breakout remains a close above confirmed resistance; a two-ATR rise from a floor is momentum, not a breakout. A rejected peak now becomes an active ceiling as soon as its close-based reversal reaches the configured peak-day ATR threshold. Later returns into the same half-ATR area increase the ceiling's test count instead of being required to create it. A distant older ceiling is retained as major overhead context rather than blocking the local signal. If price rises at least two ATR from the active floor before any local ceiling is confirmed, report `STRONG_REBOUND` instead of inventing a breakout.

Historical validation note: the former synthetic HFCL fixture creates only a 0.88 ATR peak-to-close rejection under pivot-anchored ATR. With the new strict symmetric 1.0 ATR default it therefore retains ₹231.41 as major overhead but does not invent the ₹195.53 local ceiling or a 3 August breakout. This changed result is intentional and should be compared against the real HFCL replay during manual validation.

2026-08-16 structural-model revision: align the replay with a causal ATR ZigZag. Both floor and peak turns use a symmetric 1.0 ATR threshold anchored to the candidate extreme's date after the ATR warm-up. A rejected peak becomes an active ceiling immediately; later returns into the zone increase its test count instead of being required to create it. Every candle must update structure independently of the one primary decision shown in the replay, a breakout invalidates the ceiling it clears, and an initial candle that simultaneously confirms both directions is marked ambiguous rather than assigned an invented order. The half-ATR ceiling allowance, three-ATR major-overhead rule, and two-ATR non-breakout `STRONG_REBOUND` remain unchanged pending manual chart validation.

Raw-replay comprehension follow-up: the audit drawer must teach the price story without requiring the user to memorize engine terminology. Keep every raw value, but add an explicit bottom-to-top reading path, a compact stage sequence, consistent decision colors, plain-language summaries that compare the close with the active ceiling, and descriptive labels for the local ceiling (`line to beat`) versus major overhead (`later obstacle`). Avoid scores, hidden rows, or additional strategy logic; this is an interpretation-layer change only.

Implemented and visually checked at a 1,280px viewport. The raw table keeps all original fields and explanations, fits without losing columns, uses text plus color for every event state, and exposes the simple row-level meaning before the technical engine wording.

Daily-shape follow-up: add a compact visual in every raw row showing the session's high-low range and the relative open/close positions, with green for close above open, red for close below open, and neutral for equal prices. The graphic must be accessible and explicitly avoid implying whether the high or low occurred first, because daily OHLC data does not contain that intraday sequence.

Implemented and validated with focused tests, a production build, and live review at 1,280px. The glyph keeps every audit column visible and announces the exact OHLC values plus the unknown intraday order to assistive technology.

ATR-comprehension follow-up: remove the need to manually subtract prices and divide by ATR when reading a turn. On `CEILING_CONFIRMED` rows, show the downward move from candidate peak to close against the peak-day ATR and the 1.00 ATR trigger. On `FLOOR_CONFIRMED` rows, show the upward move from candidate floor to close against the floor-day ATR and the 1.00 ATR trigger. Keep this inside the existing explanation cell so no audit column is lost, and use arrows plus words so color is not the only meaning.

Each relevant row now presents the subtraction, rupee movement, anchored ATR multiple, required multiple, pass mark, and resulting state; downward turns also identify the new candidate low. Focused tests and the production build pass with the revised engine.

Manual-validation follow-up: every `Already broken` stock must show its last fresh-breakout date directly below the status badge. Use the persisted ceiling/evidence date rather than searching the bounded raw replay, include the year for historical testing, and state when the date is unavailable instead of silently hiding the field.

Compact-ceiling correction: retain the existing 1.0 ATR peak-to-close rejection as the immediate, high-confidence ceiling path. Add a separate compact-range path for shallower but persistent rejection: a peak that closes at least 0.5 peak-day ATR below its high becomes a candidate, and it becomes the active local ceiling only after the next two completed sessions fail to print a higher high. Any higher high before confirmation replaces the candidate and restarts the two-session wait. The existing breakout line remains `peak + 0.5 ATR`; the first later close strictly above it is the fresh breakout. When this newer local ceiling is activated, a non-overlapping older higher ceiling becomes major overhead rather than blocking the local breakout.

KRN is the acceptance case. The 04 Aug peak near ₹1,230 and roughly 0.68 ATR rejection starts the compact candidate; highs on 05 and 06 Aug remain contained, so the local ceiling is confirmed on 06 Aug. Its upper breakout line is approximately ₹1,254.52. The 10 Aug high remains a test inside that zone, while the 11 Aug close near ₹1,287.70 is the fresh breakout. The UI and raw replay must name whether a ceiling came from `STRONG_REJECTION` or `COMPACT_RANGE` and show compact confirmation progress without requiring mental arithmetic.

Early-breakout follow-up: do not weaken the confirmed-ceiling or `FRESH_BREAKOUT` rules to solve the two-session confirmation delay. A completed close that crosses a live compact candidate's peak plus 0.10 of the candidate-day ATR is a separate `EARLY_BREAKOUT` watch event. It is lower confidence, does not activate or break a ceiling, does not create breakout evidence, is excluded from fresh-breakout counts and backtests, and remains `CONTEXT_ONLY` in Buy Review. The scanner must show the provisional early line and retain any older confirmed ceiling as context. NETWEB's 15 Jun ₹4,744 candidate produces an early line near ₹4,770.21, so the 16 Jun ₹4,884.40 close is the reference case.
