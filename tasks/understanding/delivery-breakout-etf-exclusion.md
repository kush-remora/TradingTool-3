The Delivery Breakout Validation screen should stop showing ETFs. Kush has added a local NSE ETF reference file at `manual-input/eq_etfseclist.csv`, and the current backend does not have any first-class stock-vs-ETF identifier in `stock_delivery_daily` or from Kite that can be trusted for this purpose.

The simplest fix is to keep classification local to this feature for now: load ETF symbols from `manual-input/eq_etfseclist.csv`, normalize them, and exclude matching symbols during Delivery Breakout dashboard assembly before ranking. The implementation now does that through a small `DeliveryBreakoutEtfService`, keeps fail-open behavior if the file is missing or malformed, and has focused tests covering CSV parsing plus ETF-row exclusion. Validation passed with focused core tests and a full `service` compile.
