# Compact review data freshness

Daily candles and delivery records can have different completion dates. The compact header now labels the latest candle as `Close <date>` and shows `Delivery as of <date>` in the delivery cell, keeping delayed delivery data visible without adding an interpretation or warning state.
