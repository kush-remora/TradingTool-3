# 5-Day Stock Selector

## Why

The existing Compact Stock Review is the detailed evidence screen. Kush needed a lighter watchlist-level screen that quickly identifies stocks worth opening when the intended trade is one to five days with a 5% to 10% objective.

## Implemented

- Added a `5-Day Stock Selector` console route and sidebar entry.
- Reused the existing daily watchlist scan; no new backend endpoint was needed.
- Added a compact table with the latest close, historical `+5% in 5D` count/rate, latest close position from low to high, and direction.
- Added two tabs: the full watchlist and an adaptive Shortlist view.
- Added a Core tab containing only stocks selected by both the 20-day and recent 6-day rankings.
- Added both the full 20-day and recent 6-day successful-day counts to the full-watchlist view.
- Added a minimum evidence gate before ranking: at least 5 successful days out of 20 and at least 2 successful days out of the recent 6 eligible days. A stock cannot enter just because it ranks highly in a weak watchlist.
- Added a price-position guard before ranking: the latest close must be no more than 10% below the recent 20-session high. This removes stocks where the recent move may already be materially weak or late.
- Added a current-weakness guard: reject only when the last 3 closes fall in a row and the latest close breaks below the previous 5-session low. Three red candles alone do not reject a stock.
- Core retains its original top-20%-per-ranking overlap behavior; Tab 2 Shortlist is now separate and does not use that cap.
- Added Shortlist context fields for the recent 20-session high, signed pullback from that high, and the largest volume bar in the latest five sessions versus the prior 20-session average.
- Added Core context for the 52-week high and distance from it; this orders the Core list but is not another rejection rule.
- Added current 5-session and 20-session price moves to Core, with direction arrows; these are context only and do not change membership.
- Added Core context for `Strong finishes`: the count of the latest five completed candles whose close finished above 60% of that candle's high–low range. This is descriptive pressure evidence only and does not change membership or ranking.
- Added Core context for `Move quality`, classifying the latest five candles as `Clean`, `Mixed`, or `Wild` using close progression, strong finishes, direction changes, and path efficiency. It is context only and does not change membership or ranking.
- Made `Latest finish` sortable by the close's percentage position within the latest candle's high–low range.
- Reworked the `Details` modal to show the latest 20 completed sessions, newest first, with date, OHLC, close-to-close change, close position within the daily range, and distance from the day's high. Historical 5D reach examples are no longer the primary detail view.
- Made the read-only `Details` modal modeless by removing its blocking mask, so the global buy/sell calculator remains usable while details are open.
- Clarified the reach terminology in the table, rule explanation, method note, and details modal: `5D reach` means price touched +5% within the next five trading sessions from a tested starting close; `Recent tested 6D` means the six latest eligible starting days.
- Added a sortable `Strong finishes` column to All Stocks, showing the count out of five plus five filled/empty dots in newest-first order. Filled dots mean the candle closed in the upper 40% of its daily range; hover still exposes the date and sequence position.
- Added one sortable `Move now` column to All Stocks with non-overlapping `Now 5D`, `Prior 5D`, and `Earlier 10D` close-to-close movement. The cell summarizes latest-versus-prior-5D pace and keeps the hidden 20D total and extension detail in hover text.
- Added a sortable `Exit pressure` warning to Shortlist and Core. It now evaluates a three-session push/failure sequence against the preceding 10-session volume average: `Quiet`, `Watch`, or `Caution` when a meaningful 1.5x strong-close push is followed by weak failure. It remains context only and does not imply confirmed distribution or change selection.
- Validation: focused selector tests passed with 20 tests, and the production frontend build passed. The page test confirms the column appears once in Shortlist and Core and remains sortable.
- Reordered All Stocks so 10D/20D move, Strong finishes, Move quality, and Exit pressure lead the reading order; 5D reach remains visible as historical context. Added a `Volume vs 10D avg` multiplier to the recent-details popup.
- Added a `How to read Tab 1` button that loads a backend-owned JSON guide into a reusable popup table. The guide source is `core/src/main/resources/short_horizon_selector/tab_one_guide.json`, and the endpoint/UI tests plus frontend build and backend module compile passed.
- Reworked the All Stocks move column into a compact stacked `Move now` cell with `Now 5D`, `Prior 5D`, and `Earlier 10D` shown one below the other. Green/red color communicates each bucket's direction, an arrow compares the latest 5D with the prior 5D using a 1 percentage-point tolerance, and the Latest close cell shows a small amber warning when the hidden 20D total is above 25% for extension review. Detailed total and bucket math remains available on hover to reduce table clutter.
- Reduced visual weight in the stacked `Move now` cell: repeated `5D`, `10D`, and `20D` labels are now small and light, while the percentage values remain prominent to reduce row-level cognitive load.
- Placed `Strong finishes` beside `Latest finish` on Tab 1. Reduced the Latest finish column width so repeated five-session buyer control and the most recent candle can be compared together without expanding the table.
- Added `20D` total movement and distance from the recent high under `Latest close`. An amber caution symbol appears only when the 20D move is strictly above 25%; it is an extension warning and does not filter or reject the stock.
- Refined `Exit pressure` to model the Composite Man sequence: a strong-close volume push can attract buyers, but the next-session failure creates Caution only when the push volume reaches 1.5x the preceding 10-session average. A strong close followed by weak low-volume follow-through remains Watch, not confirmed supply.
- Reworked the Shortlist tab as the first filter-driven reading pass. It now shows the Tab 1 evidence columns (`Move now`, `Strong finishes`, `Latest finish`, `Move quality`, `Exit pressure`, `Latest close`, and `5D reach`) and adds filters for accelerating pace, minimum strong finishes, and exit pressure.
- The Shortlist defaults to accelerating `Now 5D` versus `Prior 5D`, allows `Any`, `Steady`, `Recovering`, or `Weakening` as alternatives, leaves strong finishes as an optional minimum-count filter, and excludes only `Exit pressure = Caution`; `Watch` remains visible.
- Kept the three-declining-closes-plus-previous-five-session-low-break guard in the Tab 2 current filter. Historical reach thresholds and the recent-high guard are now ranking context for Tab 2, not gates applied before the current-condition filter.
- Corrected the Shortlist explanation so it does not present the historical reach thresholds as the agreed current-reading logic. It now distinguishes the existing historical candidate pool from the current Tab 1 filters: accelerating Move now, optional Strong finishes, Exit pressure Caution exclusion, and the two-part structural weakness guard.
- Corrected the Tab 2 pipeline order after validating a 99-stock scan: current-condition filters now run across the full watchlist first, and historical 5D/recent-6D reach is used for ranking the survivors. The old 5/20, 2/6, and recent-high checks no longer remove stocks before the current Tab 1 filter pass.
- Removed the old top-20%-per-ranking cap from Tab 2. Every stock that passes the current-condition filters remains visible; historical reach still determines ordering, but no current-condition candidate is hidden by the ranking limit.
- Added the sortable `52W high` distance context column to Tab 2. It shows distance from the 52-week high and the high price, but does not filter candidates.
- Added Tab 1 column filters for Move now acceleration, Strong finishes, Latest finish, Move quality, Exit pressure, and the 20D extension warning. These filters are presentation-level exploration on All Stocks and do not alter Tab 2 or Core membership.
- Split the Shortlist explanation into separate numbered `Sorting rules` and `Filtering rules` sections so historical ranking is not confused with current-condition filtering.
- Added a `Review` action that opens the existing Compact Stock Review.
- Added pure calculation tests and a page-flow test.

## Decisions and tradeoffs

- A successful example means the next five available trading-session highs reached at least 5% above the starting close.
- Only the latest 20 usable starting days are considered; the denominator is shown when fewer are available.
- A close at or above 75% of the day’s low-to-high range is `HIGH`, at or below 25% is `LOW`, and the rest is `MID`.
- The screen intentionally has no combined score, buy label, rejection metric, 10% metric, or intraday data.
- The volume and pullback fields are descriptive evidence only; a large volume bar is not labelled confirmed distribution. The three-close breakdown rule is the current structural weakness guard.

## Validation

- Focused selector tests: 20 passed.
- Frontend production build: passed.
- Full frontend suite: 149 passed, 17 existing failures across 10 unrelated test files, mostly timeout/stale assertion failures. The new selector tests passed in the full run.
- Wording-only update validation: focused selector tests passed after replacing ambiguous `20D` / `6D` labels with explicit reach terminology.

## Next follow-ups

- Review the live visual density and labels.
- Decide whether to add the 10% target as a secondary detail only.
- Define give-back/rejection behaviour before adding it to the UI.
- Review the Core tab visually and confirm that the smaller overlap list is useful before adding another filter.
- Validate the default Tab 2 acceleration and `Caution` exclusion against historical examples before changing thresholds or filtering `Watch`.

## Implemented Best aligned tab and 52W high age

Added a strict `Best aligned` tab that filters the full computed watchlist using only two current-condition rules: `Move now = Accelerating` and `Strong finishes >= 2 / 5`. Move quality remains visible context but no longer affects membership. The tab does not inherit Shortlist's Exit pressure or structural-weakness filters. The existing 52W-high context now also includes the high date and how many trading sessions ago it occurred.

The 52W-high age is calculated from the backend's full 252-session history rather than the frontend's visible recent-details window, so an older high is not incorrectly reported as unavailable. The 52W high remains context only and does not affect Best aligned membership.

Validation for this addition: focused selector tests passed with 24 tests and the production frontend build passed. The backend Kotlin test could not be run in this environment because no Gradle wrapper or `gradle` executable is available.

## Added delivery percentage to recent details

The modeless Recent 20D details popup now includes a sortable `Delivery %` column sourced from each daily candle. It is shown alongside volume versus the preceding 10-session average, so price/volume activity and delivery participation can be compared session by session without changing any selection rule.

## Added Best aligned · Quiet tab

Added a stricter `Best aligned · Quiet` tab derived from Best aligned. It keeps the two Best aligned rules—Accelerating Move now and at least `2 / 5` Strong finishes—and additionally requires `Exit pressure = Quiet`. Move quality and 52W-high distance remain visible context only.

## Clarified Move now states

`Accelerating` now requires a positive Now 5D move that improves by at least 1 percentage point versus Prior 5D. A negative-to-less-negative sequence is labelled `Recovering`, not acceleration; a materially worse latest pace is `Weakening`. This prevents a stock such as Now 5D `-1.5%` versus Prior 5D `-5.2%` from entering the upward-acceleration view.

## Reworked Exit pressure as a volume-first warning

Exit pressure now uses recent volume events rather than requiring the event candle to close in the upper part of its range. The same preceding 10-session average is used for every event. `Quiet` means no qualifying volume event; `Watch` means volume reaches at least `2×` without confirmed failure; `Caution` means volume reaches at least `1.5×` and the next session closes lower with weak follow-through, at least a 1% decline, or a break below the event low. Strong finishes remains the separate buyer-control metric. The UI now shows `No qualifying volume event` when Quiet has no event, instead of displaying a misleading low-volume push.

## Neutral volume activity and next-level filtering reset

- Replaced the interpretive `Exit pressure` field with neutral `Volume activity`.
- `Quiet` means no event in the latest three sessions reached `1.5×` its preceding 10-session average; `Watch` means at least one did. The signal calls out abnormal participation only and does not classify it as entry or exit.
- Removed the Tab 2 `Caution` exclusion because there is no longer a Caution state.
- Removed the `Best aligned · Quiet` and `Core` tabs. `Best aligned` remains as the current next-level filtering starting point while its next rules are revisited.
- Updated the Tab 1 guide, table filters, tests, and explanations to use Volume activity.

## Added Best aligned minimum speed evidence

Best aligned now rejects stocks with no demonstrated short-horizon speed. A row must have either `5D reach >= 3 / 20` or `Recent tested 6D reach >= 1 / 6`, then it must satisfy the existing `Move now = Accelerating` and `Strong finishes >= 2 / 5` rules. All Stocks and Shortlist behavior remain unchanged.

Added a separate `Latest 2-day finish` tab derived from Best aligned. It requires at least one of the latest two completed candles to close at least 75% up its daily range. This is a minimum close-strength condition; closes in the 80–90% area are stronger evidence, while the 75% threshold avoids making the original Best aligned tab unnecessarily strict.

## Added final-tab context filters

The final `Latest 2-day finish` tab now enables column filters for `Move quality` and `Volume activity`. This lets Kush reduce the final list interactively, for example by showing only Clean movers or only Quiet/Watch volume activity, without changing the tab's base selection logic. Best aligned and Shortlist membership rules are unchanged.

## Added day change to Latest close

The `Latest close` cell now shows the latest completed candle's close-to-close percentage change beside the price, for example `Day +2.4%`. The existing date, 20D move, distance from recent high, and extension warning remain in the same cell. This is display context only and does not change any tab rule.

## Tightened Latest 2-day finish freshness

The `Latest 2-day finish` tab now rejects stale historical winners. A stock can still enter `Best aligned` with strong broader history, such as `5D reach 6 / 20`, but it cannot enter the final latest-finish tab unless `Recent tested 6D` is at least `1 / 6`. After that fresh-proof gate, the existing latest-two-candle close-strength rule still applies.
